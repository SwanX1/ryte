{{#if session_user}}
  <div class="segmented-control">
    <button class="segment segment-feed">Feed</button>
    <button class="segment segment-followers">Recent</button>
  </div>
  <h1 class="welcome">Welcome back, {{session_user.username}}!</h1>
  <hr><br>
  <div class="card">
  </div>
{{else}}
  <div class="card">
    <p>Please <a href="/auth/login">login</a> or <a href="/auth/signup">sign up</a> to start posting.</p>
  </div>
{{/if}}
